export interface PollingOptions { intervalMs?:number; onPoll:()=>Promise<void>; onError?:(error:unknown)=>void; }
export class PollingLoop { private timer?:NodeJS.Timeout; private running=false; constructor(readonly options:PollingOptions){} start(){if(this.timer)return;this.timer=setInterval(()=>void this.tick().catch(e=>this.options.onError?.(e)),this.options.intervalMs??15000);void this.tick().catch(e=>this.options.onError?.(e));} private async tick(){if(this.running)return;this.running=true;try{await this.options.onPoll()}finally{this.running=false}} stop(){if(this.timer)clearInterval(this.timer);this.timer=undefined;} }
