# ACCEPTANCE v0.3

状态（2026-09-23）：v0.3.8 进一步修复对 Codex 暂态 `interrupted` 的误判，并在当前 Mac 安装验证；真实 TaskRef 授权、云端语音识别绑定与设备重启恢复已验证。修复后的从零语音配对、双向真实消息、解绑/重新绑定和全新 Mac 安装仍待现场验收。

| 项目 | 当前状态 | 证据边界 |
|---|---|---|
| P01 arm64 GitHub 新机安装 | v0.3.8 已发布/新机待现场 | public 仓库、v0.3.8 Release、固定 bootstrap 和 sidecar 已校验；新 Mac 现场待验 |
| P02 账号与两个任务 | 当前 Mac 部分通过/新机待现场 | 演示账号已关联，当前真实 TaskRef 已授权并显示；第二个任务和新机仍待验 |
| P03 语音配对 | 当前设备部分通过/修复后从零待验 | `demo-usb-01` 语音尝试于 11:38:30 CST 变为 `bound`；云端绑定 `bind-ab01892f-ca60-454d-84a5-2b4a6768c005` 为 `active`。设备画面靠修复版重启恢复后显示当前对话，尚非修复后同轮无重启闭环 |
| P04 重启保留绑定 | 当前设备通过/新机待现场 | 修复固件刷入并重启后，设备调用 binding recover 取回同一绑定；11:45:34 CST 云端收到该绑定的设备视图，用户确认屏幕显示“开发 Codex 硬件助手 v0.3” |
| P05–P07 双向消息 | 完成状态下行部分通过/双向消息待验 | 用户确认设备显示上一轮任务“完成”；Codex 上一轮 turn 确为 completed。既有 queue、任务快照、目标冻结实现保留；未发送新真实任务 |
| P08–P09 解绑/再绑定 | 主机验证/待真机 | prepare/confirm 和旧状态隔离已实现；未真机 |
| P10 后台运行 | 当前 Mac 部分通过/新机待现场 | v0.3.7 修复启动和重连；v0.3.8 已在当前 Mac 正常升级，16:48 CST 云端 channel 在线、心跳更新，配置与原绑定不变；重新登录/新机仍待现场 |
| P11 离线解绑 | 待现场 | 云端 `deviceAppliedState=pending` 契约已实现；未设备离线现场 |

本轮配对故障：录音上传获 202 后，固件绘制“正在识别配对码”，却未从 `RECORD_CODE` 转为 `MATCHING`，因此没有轮询已绑定结果。现已补齐状态转换，并在设备重启时恢复云端既有绑定；未重新念码、未解除当前绑定。本轮没有向真实 Codex 任务发送测试消息。此处仅记录当前开发机与当前设备的证据，不替代全新 Mac 验收。

2026-09-23 当前设备曾显示的“失败”来自 Connector 读到的最新 turn 状态 `interrupted`；同一时刻 Codex 桌面任务状态为 `inProgress`。上一轮结束后，用户确认设备显示“完成”，Codex 对应 turn 也确为 completed。新一轮启动时误报“失败”再次出现，v0.3.8 不再把 `interrupted` 暂态推断为 failed：当前 Mac 安装后，云端任务结果从 failed 改为 unknown。设备侧 v0.3.8 画面尚待用户确认；双向消息验收不得据此判为通过。设备现有绑定保持 active，未执行解绑。
