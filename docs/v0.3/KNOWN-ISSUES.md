# v0.3 已知问题

- 没有新 Apple 芯片 Mac 时，P01、P02 的双任务部分及 P05–P11 的新机流程仍待现场；当前开发机/设备的配对成功不能代替新机验收。
- 当前设备的云端语音识别绑定和重启恢复已验证。此前“正在识别配对码”卡住是固件上传后未推进状态机，云端实际已绑定；修复版已刷入并恢复现有绑定。尚未验证修复后不重启的从零配对、解绑后的重新绑定。
- LaunchAgent 的 `loaded`、`running`、云端心跳和任务快照是三个独立状态，不能只看 `launchctl`。
- 休眠期间不承诺同步；唤醒后依赖 Connector 自动重连。
- 本期解绑保留 Codex 原任务和云端历史，不等同于完整二手转让清理或遗失恢复。
