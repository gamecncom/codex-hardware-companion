# Hardware Companion Skill

Install this folder from the published GitHub release on an Apple-silicon Mac, then invoke `$hardware-companion`. The skill delegates to the verified `companion` JSON CLI and does not embed credentials or private project paths.
